used to sort based on ascending or descending order

syntax - db.document.find().sort({document body})

uses 1 for ascending order and -1 for descending order 

eg :- 
    db.students.find().sort({name: 1 or -1})

    